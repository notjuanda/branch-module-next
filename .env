# API URLs - Todas pasan por el reverse proxy nginx
NEXT_PUBLIC_API_URL=http://localhost
NEXT_PUBLIC_API_URL_2=http://localhost

# El chatbot ahora también pasa por nginx (puerto 80)
# Ya no se usa NEXT_PUBLIC_API_URL_3 porque todo pasa por nginx
